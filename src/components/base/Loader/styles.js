import { StyleSheet } from 'react-native';

export default function styles() {
    return (
        StyleSheet.create({
            container : {
                justifyContent : 'center',
                alignItems     : 'center'
            }
        })
    );
}
