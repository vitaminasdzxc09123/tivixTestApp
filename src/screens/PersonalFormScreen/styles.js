import { StyleSheet } from 'react-native';

export default function styles({}) {
    return (
        StyleSheet.create({
            itemContainer : {
                marginTop : 12
            }
        })
    );
}
